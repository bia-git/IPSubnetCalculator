<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base theme is defined entirely in Compose (Theme.kt). This XML theme
         is only used for the launch / splash window background. -->
    <style name="Theme.IPSubnetCalculator" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/purple_700</item>
        <item name="android:windowBackground">@color/white</item>
    </style>
</resources>
